name: Debug MV Header
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump MV Header
        run: |
          echo "=== MV Header ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/music-videos-header"
          echo ""
