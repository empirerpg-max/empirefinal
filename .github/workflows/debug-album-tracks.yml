name: Debug Album Tracks
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump Album Tracks
        run: |
          echo "=== Album Tracks ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/album-tracks"
          echo ""
