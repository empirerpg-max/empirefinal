name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/raw-write-test" | tee /tmp/out.json
