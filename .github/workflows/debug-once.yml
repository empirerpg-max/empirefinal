name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: |
          curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/empire-play/home" | jq -c '{spotify: .data.topSpotify[0], apple: .data.topAppleMusic[0], youtube: .data.topYoutube[0]}'
