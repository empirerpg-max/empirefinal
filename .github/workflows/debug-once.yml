name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: curl -sS -X POST --max-time 120 "https://tanstack-start-app.empirerpg-forum.workers.dev/api/artistas/calcular-fortuna-charts" | tee /tmp/out.json
