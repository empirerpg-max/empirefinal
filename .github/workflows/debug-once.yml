name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: |
          echo "=== agora (UTC) ==="
          date -u
          echo "=== /api/flags ==="
          curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/flags" | tee /tmp/out.json
