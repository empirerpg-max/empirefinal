name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl sw.js headers
        run: curl -sSI "https://tanstack-start-app.empirerpg-forum.workers.dev/sw.js?v=$(date +%s)"
      - name: Curl sw.js body
        run: curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/sw.js?v=$(date +%s)"
