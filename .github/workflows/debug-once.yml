name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: |
          echo "=== agora (UTC) ==="
          date -u
          echo "=== teste upload GIF (tvChatGifs) ==="
          curl -sS -X POST "https://tanstack-start-app.empirerpg-forum.workers.dev/api/gestao/upload" \
            -H "Content-Type: application/json" \
            -d '{"fileName":"teste-debug.gif","mimeType":"image/gif","folderType":"tvChatGifs","base64Data":"R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw=="}'
