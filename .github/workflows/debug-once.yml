name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl registro debug
        run: curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/registro"
