name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/charts?action=getFilters&tab=DADOS%20%C3%81LBUNS" | tee /tmp/out.json
