name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/qual-service-account" | tee /tmp/out.json
