name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: |
          echo "=== agora (UTC) ==="
          date -u
          echo "=== /api/turnes (buscando show Paradiso #7) ==="
          echo "=== reverter show automatico (Marco, show 7) ==="
          curl -sS "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/tour-reverter-automatico?idUnico=Marco1065444507_1787131362340&showNumero=7"
