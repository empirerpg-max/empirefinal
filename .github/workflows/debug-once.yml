name: Debug Once
on:
  workflow_dispatch: {}
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Curl debug endpoint
        run: curl -sS -X POST "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/criar-musicas-import" | tee /tmp/out.json
