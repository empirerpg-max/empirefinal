name: Debug Meus Artistas
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump Usuários (Hugo)
        run: |
          echo "=== Usuários (nome contém 'hugo') ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?sheet=Usu%C3%A1rios&filterCol=nome&filterVal=hugo"
          echo ""
