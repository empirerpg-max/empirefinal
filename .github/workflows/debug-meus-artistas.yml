name: Debug Meus Artistas
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump ARTISTAS
        run: |
          echo "=== ARTISTAS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?sheet=ARTISTAS"
          echo ""
