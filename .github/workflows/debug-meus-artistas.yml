name: Debug Meus Artistas
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch tabs
        run: |
          echo "=== TABS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/tabs"
          echo ""
