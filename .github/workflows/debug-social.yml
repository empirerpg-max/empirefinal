name: Debug Social Tabs
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump SOCIAL_PERFIS
        run: |
          echo "=== SOCIAL_PERFIS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?spreadsheet=usuarios&sheet=SOCIAL_PERFIS"
          echo ""
      - name: Dump SOCIAL_POSTS
        run: |
          echo "=== SOCIAL_POSTS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?spreadsheet=usuarios&sheet=SOCIAL_POSTS"
          echo ""
      - name: Dump SOCIAL_COMMENTS
        run: |
          echo "=== SOCIAL_COMMENTS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?spreadsheet=usuarios&sheet=SOCIAL_COMMENTS"
          echo ""
      - name: Dump SOCIAL_NEWS
        run: |
          echo "=== SOCIAL_NEWS ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/dump?spreadsheet=usuarios&sheet=SOCIAL_NEWS"
          echo ""
