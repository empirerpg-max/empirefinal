name: Debug Pontos
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump Pontos
        run: |
          echo "=== Pontos ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/pontos"
          echo ""
