name: Debug Edicao Charts
on:
  workflow_dispatch: {}
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Dump Edicao Charts
        run: |
          echo "=== Edicao Charts ==="
          curl -s "https://tanstack-start-app.empirerpg-forum.workers.dev/api/debug/edicao-charts"
          echo ""
